MONGODB_URI=
JWT_SECRET=
GEMINI_API_KEY=

# Comma-separated frontend URLs allowed by CORS.
# Use your exact Vercel production URL, and optionally the wildcard for Vercel preview deployments.
CLIENT_ORIGIN=http://localhost:5173,https://your-frontend.vercel.app,https://*.vercel.app

PORT=3000
